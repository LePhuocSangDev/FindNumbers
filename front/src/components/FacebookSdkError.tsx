import React from "react";

const FacebookSdkError = () => (
  <div>
    <p>
      It looks like there is a content blocker or other extension that is
      preventing the Facebook SDK from being loaded. Please disable the
      extension and try again.
    </p>
  </div>
);

export default FacebookSdkError;
